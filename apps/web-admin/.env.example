NEXT_PUBLIC_API_BASE_URL=https://api.booking-villa.example.com/api
NEXT_PUBLIC_PUBLIC_APP_URL=https://booking-villa.example.com
# Active payment flow uses Xendit. Legacy hosted-page config has been removed.
SESSION_COOKIE_NAME=admin_session
# Set only for local development bypass. Must stay `0`/unset in staging and production.
NEXT_PUBLIC_DEV_NO_AUTH=0
