/**
 * DO NOT USE - legacy.
 * Mock Super Admin sudah diarsipkan ke:
 * `Booking-Villa/archive/web-admin/super-admin/mocks.legacy.ts`
 *
 * File stub ini sengaja dibiarkan kosong agar:
 * 1. tidak ada lagi data static yang bisa dipakai flow aktif
 * 2. import lama cepat terdeteksi saat development
 * 3. project aktif tidak lagi memuat mock usang
 */

export {};
